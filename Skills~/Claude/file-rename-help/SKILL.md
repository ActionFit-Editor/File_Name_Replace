---
name: file-rename-help
description: Explain File Name Replace target selection, replace, prefix, suffix, deletion preview, Unity menus, and destructive-operation boundaries without changing files.
---

# File Name Replace Help

Answer in the user's language. Explain the workflow without selecting folders, opening the tool, renaming or deleting files, or moving Unity assets.

1. Read `PACKAGE_SKILLS.md` first. Treat its generated package identity, complete related-skill table, `$skill-name` invocations, descriptions, and access values as authoritative.
2. Read `Packages/com.actionfit.filenamereplace/README.md` and `AI_GUIDE.md` when available. If downloaded, resolve `Library/PackageCache/com.actionfit.filenamereplace@*` without editing it.
3. Explain target-folder selection, Before-to-After replacement, prefix and suffix addition, keyword deletion, and the user review expected before execution.
4. List `Open Window` and `README` under `Tools > Package > File Name Replace`.
5. State that this help-only skill never runs rename or delete actions, edits `.meta` files, changes Project selection, or modifies asset references.
